// Scheduler stubs — Phase 3
// Will poll saved routes on a configurable interval and save price snapshots.

export function startScheduler(): void {
  console.log('[scheduler] Scheduler not yet active (Phase 3)');
}
